<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="psl.ncx.reader"
    android:versionCode="1"
    android:versionName="1.0" >

    <uses-sdk
        android:minSdkVersion="14"
        android:targetSdkVersion="18" />

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.MOUNT_UNMOUNT_FILESYSTEMS" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme" >
        <activity
            android:name="psl.ncx.reader.MainActivity"
            android:label="@string/app_name"
            android:theme="@style/ActionBarOverlayTheme"
            android:windowSoftInputMode="adjustNothing" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="psl.ncx.reader.SearchResultActivity"
            android:label=""
            android:icon="@drawable/navigation_back">
        </activity>
        <activity
            android:name="psl.ncx.reader.ChapterActivity"
            android:label="@string/title_activity_chapter"
            android:theme="@style/FullscreenTheme" >
        </activity>
        <activity
            android:name="psl.ncx.reader.ContentActivity"
            android:label=""
            android:icon="@drawable/navigation_backhome"
            android:theme="@style/FullscreenActionBarTheme" >
        </activity>
        <activity
            android:name="psl.ncx.reader.SummaryActivity"
            android:label=""
            android:icon="@drawable/navigation_back">
        </activity>
        <service android:name="psl.ncx.reader.service.DownloadService"></service>
    </application>

</manifest>
